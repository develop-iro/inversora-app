/** Top fund per benchmark group for the rankings index (group metadata + best score). */
export const RANKINGS_GROUP_INDEX_LIMIT = 1;

/** Funds per group loaded on home for theme filters and the Ranking tab. */
export const RANKINGS_HOME_GROUP_LIMIT = 10;

/** Benchmark groups requested from `GET /rankings` when listing themes (matches API default). */
export const RANKINGS_DEFAULT_GROUPS_LIMIT = 24;

/** Funds returned for a single benchmark detail screen. */
export const RANKINGS_BENCHMARK_DETAIL_LIMIT = 100;
