name: Preview build (Android)

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android preview
    type: build
    params:
      platform: android
      profile: preview
