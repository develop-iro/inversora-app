# Required for Metro/Babel resolution on EAS Build with pnpm.
# See: https://docs.expo.dev/guides/monorepos/#pnpm
node-linker=hoisted
# Mitigates ENOENT on OneDrive during pnpm atomic installs (Windows).
package-import-method=copy
